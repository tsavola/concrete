Dependencies:

  g++ 4.4
  svn           (for downloading boost)
  python 3.1    (for compiling to bytecode)
  libgtest      (for unit tests)

  For Mac OSX:
    Install gcc46 and boost from macports (http://www.macports.org).

Build instructions:

  git submodule update --init
  make

  For Mac OSX:
    Create config.mk at the top-level directory and put this in it before
    running make:
-- snip (config.mk) --
CC = gcc-mp-4.6
CXX = g++-mp-4.6
-- snip (config.mk) --
